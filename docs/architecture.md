# 系统架构设计

## 整体架构

```
┌─────────────────────────────────────────────────────────────────┐
│                          客户端层                                │
│  微信小程序  Android  iOS  HarmonyOS  Web  Windows/Mac/Linux     │
└────────────────────────────┬────────────────────────────────────┘
                             │ HTTPS / WSS
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                        接入层 (Port 29090)                       │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌───────────────┐   │
│  │  Gin Router │  │  JWT Auth  │  │ RateLimit │  │  Swagger Doc  │   │
│  └──────────┘  └──────────┘  └──────────┘  └───────────────┘   │
└──────────────┬──────────────────────────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────────────────────────┐
│                     业务服务层 (微服务/模块)                      │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │             API Gateway / 统一入口                        │   │
│  └────────────┬─────────────┬──────────────┬───────────────┘   │
│               │             │              │                    │
│       ┌───────▼───────┐ ┌──▼───┐  ┌───────▼───────┐            │
│       │  Service S0   │ │ S1   │  │  Service S2   │  ...       │
│       │ (调试/调测)    │ │(自建) │  │  (自建)       │            │
│       └───────┬───────┘ └──┬───┘  └───────┬───────┘            │
│               │             │              │                    │
│               └─────────────┴──────────────┘                    │
│                              │ gRPC / Event                    │
│                              ▼                                  │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                    公共服务层                              │   │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌────────────┐  │   │
│  │  │  Auth    │ │  User    │ │  Notify  │ │  File      │  │   │
│  │  │  Service │ │  Service │ │  Service │ │  Service   │  │   │
│   │  ┌──────────┐ ┌──────────┐ ┌──────────────┐ ┌─────────────┐│   │
│   │  │  UPS     │ │  LAN Ctl │ │  Scheduler    │ │  Backup     ││   │
│   │  │  Service │ │  Service │ │  Service      │ │  Service    ││   │
│   │  ┌──────────┐ ┌──────────────┐ ┌──────────────┐ ┌─────────┐│   │
│   │  │ Migration│ │  Discovery   │ │  Crypto      │ │  AI         ││   │
│   │  ┌──────────┐ ┌──────────┐ ┌──────────────┐ ┌─────────────┐│   │
│   │  │  Logger  │ │  Monitor │ │  Config      │ │  Webhook    ││   │
│   │  │  Service │ │  Service │ │  Service     │ │  Service    ││   │
│   │  ┌──────────┐ ┌──────────┐ ┌──────────────┐ ┌─────────────┐│   │
│   │  │  Net     │ │          │ │              │ │             ││   │
│   │  │  Service │ │  (更多…)  │ │              │ │             ││   │
│   │  └──────────┘ └──────────────┘ └──────────────┘ └─────────┘ │   │
│  │  ┌──────────────────────────────────────────────────────────┐ │   │
│  │  │  基础设施 / 可观测 (独立 Docker Compose)                  │ │   │
│  │  │  Prometheus    Grafana    Loki       告警                 │ │   │
│  │  └──────────────────────────────────────────────────────────┘ │   │
│  └──────────────────────────┬───────────────────────────────┘   │
└─────────────────────────────┼───────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                     基础设施层                                    │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────────┐   │
│  │   MySQL  │ │  Redis   │ │  RustFS │ │  NAS 存储         │   │
│  │ (主数据库) │ │ (缓存/队列)│ │(对象存储)│ │ (MySQL/PG/Redis)  │   │
│  └──────────┘ └──────────┘ └──────────┘ └──────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

## 架构原则

### 1. 模块化 (Modular Monolith → Microservices)

- **初期**: 单体仓库 + 模块化分层，按业务领域拆分为独立 Go package
- **后期**: 按需将高负载模块抽取为独立微服务，通过 gRPC 通信
- **模块边界清晰**: 每个模块拥有自己的 handler / service / repository 三层

### 2. 依赖方向

```
Handler (路由/入参校验) → Service (业务逻辑) → Repository (数据访问)
                                                      ↓
                                              MySQL / Redis / RustFS
                                              (PostgreSQL 仅 NAS 场景)
```

- 依赖只能从外层指向内层，禁止循环依赖
- 模块间通过 `Service Interface` 解耦

### 3. 配置中心化

- 所有模块共享一个配置文件 `config.yaml`（Viper 管理）
- 环境变量覆盖配置项（12-Factor App）
- NAS 连接配置、云服务配置各自独立 section

### 4. 统一中间件链

```
Request → Logger → CORS → JWT Auth → RateLimit → Service Router → Response
```

## 目录结构规划

```
go-backend-core/
├── cmd/                    # 各服务入口
│   ├── server/            # 主服务入口
│   ├── migrate/           # 数据库迁移入口
│   └── backup/            # 备份命令行入口
├── internal/              # 私有代码
│   ├── config/            # 配置加载
│   ├── middleware/        # 通用中间件
│   ├── pkg/               # 公共工具库
│   │   ├── response/      # 统一响应
│   │   ├── logger/        # 日志封装
│   │   ├── errors/        # 错误码定义
│   │   └── crypto/        # 加解密与压缩工具
│   ├── services/          # 公共服务
│   │   ├── auth/
│   │   ├── user/
│   │   ├── notify/
│   │   ├── file/
│   │   ├── ups/           # UPS 状态监控
│   │   ├── lanctl/        # 局域网设备管理
│   │   ├── scheduler/     # 定时/周期任务调度
│   │   ├── backup/        # 数据备份与恢复
│   │   ├── migration/     # 数据库迁移管理
│   │   ├── discovery/     # 节点发现与 P2P 连接
│   │   ├── crypto/        # 加密解密与压缩解压缩
│   │   ├── ai/            # AI 推理服务
│   │   ├── logger/        # 日志服务
│   │   ├── monitor/       # 监控服务
│   │   ├── config/        # 配置管理
│   │   ├── webhook/       # Webhook 服务
│   │   ├── net/           # 网络通信服务
│   │   ├── eventbus/      # 事件总线
│   │   ├── cache/         # 缓存管理
│   │   ├── search/        # 全文搜索
│   │   ├── apikey/        # API 密钥
│   │   ├── dataio/        # 数据导入导出
│   │   ├── feature/       # 特性开关
│   │   ├── template/      # 模板引擎
│   │   ├── rules/         # 规则引擎
│   │   ├── ratelimit/     # 分布式限流
│   │   ├── mqtt/          # MQTT 协议客户端
│   │   ├── filesystem/    # 文件系统监听
│   │   ├── tagging/       # 标签系统
│   │   ├── jobs/          # 后台任务管理
│   │   ├── media/         # 媒体管理
│   │   ├── analytics/     # 报表引擎
│   │   └── plugin/        # 插件系统
│   ├── modules/           # 业务模块
│   │   ├── s0/            # 调试服务
│   │   ├── s1/            # 自建服务1
│   │   └── s2/            # 自建服务2
│   ├── nas/               # NAS 连接层
│   ├── cloud/             # 云服务连接层
│   └── database/          # 统一数据访问层
│       ├── postgres/
│       ├── mysql/
│       ├── redis/
│       ├── minio/
│       └── rustfs/
├── pkg/                   # 公开库（可被外部引用）
│   ├── apierror/
│   ├── pagination/
│   └── crypto/            # 加解密与压缩核心库
├── api/                   # API 定义 (proto/swagger)
│   ├── proto/
│   └── swagger/
├── migrations/            # 数据库版本化迁移脚本
│   ├── postgres/
│   └── mysql/
├── scripts/               # 脚本
├── deploy/                # 部署配置
│   ├── docker/
│   └── compose/
├── docs/                  # 文档
├── config.yaml            # 配置文件
├── Dockerfile
├── docker-compose.yml
├── Makefile
└── go.mod
```
